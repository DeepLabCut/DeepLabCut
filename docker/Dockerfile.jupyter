ARG CUDA_VERSION
ARG DEEPLABCUT_VERSION
FROM deeplabcut/deeplabcut:${DEEPLABCUT_VERSION}-core-cuda${CUDA_VERSION}-cudnn9

RUN pip install "notebook<7"

EXPOSE 8888
ENTRYPOINT ["jupyter", "notebook", "--no-browser",  "--ip", "0.0.0.0"]
